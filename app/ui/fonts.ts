import { Inter } from "next/font/google";
import { Lusitana } from "next/font/google";

export const FInter = Inter({ subsets: ["latin"] });
export const FLusitana = Lusitana({
  subsets: ["latin"],
  weight: ["400", "700"],
});
